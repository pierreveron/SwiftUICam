import XCTest
@testable import SwiftUICam

final class SwiftUICamTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(SwiftUICam().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
